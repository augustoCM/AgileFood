@model IEnumerable<AgileFood.Models.Fornecedores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pessoa.Nomes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pessoa.Nomes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FornecedoresId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FornecedoresId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FornecedoresId })
        </td>
    </tr>
}

</table>
